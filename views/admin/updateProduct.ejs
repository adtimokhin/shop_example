<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= pageTitle %></title>
  </head>
  <body>
    <main>
      <div>
        <form method="POST" action="/products/update">
          <!-- Title -->
          <div>
            <label for="title">Title</label>
            <input
              id="title"
              name="title"
              type="text"
              value="<%= product.title %>"
            />
          </div>
          <!-- Image URL -->
          <div>
            <label for="imageURL"
              >Enter a url for an image that would be used for this wonderful
              product of yours!</label
            >
            <input
              id="imageURL"
              name="imageURL"
              type="text"
              value="<%= product.imageURL %>"
            />
          </div>

          <!-- Description -->
          <div>
            <label for="description"
              >Write a description for this product</label
            >
            <input
              id="description"
              name="description"
              type="text"
              value="<%= product.description %>"
            />
          </div>

          <!-- Price -->
          <div>
            <label for="price"
              >What is a price of such an awesome product?</label
            >
            <input
              id="price"
              name="price"
              type="number"
              step="0.01"
              value="<%= product.price %>"
            />
          </div>

          <!-- Stock -->
          <div>
            <label for="stock">How many of this items do you have?</label>
            <input
              id="stock"
              name="stock"
              type="number"
              value="<%= product.stock %>"
            />
          </div>

          <!-- Id. Hidden -->
          <input
            type="hidden"
            name="productId"
            id="productId"
            value="<%= product._id %>"
          />

          <input type="submit" value="Submit changes" />
        </form>
      </div>
    </main>
  </body>
</html>
